apiVersion: v1
kind: Service
metadata:
  labels:
    app: referral
    environment: Development
  name: referral
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: referral
  sessionAffinity: None
  type: ClusterIP
